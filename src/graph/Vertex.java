package graph;
/**
 * 
 * Interface qui définit un sommet à partir de son label.
 *
 */
public interface Vertex {
	public String getLabel();// Renvoie le symbole (le caractère) dans le sommet (par exemple "A" ou "D" ..)

}
